# For compilation through the Makefile,
# this folder should contain the ImageJ jar file
# 'ij-???.jar' (found e.g. in Fiji.app/jars/)
# so the Java compiler has access to the classes.


For your convenience, running "make external" one folder above should trigger a download
of ij-1.51h.jar from Maven via wget

